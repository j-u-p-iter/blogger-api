const createAuthenticationService = () => {
  const hashPassword = () => {

  };

  const comparePasswords = () => {

  };

  const generateToken = () => {

  };

  const decodeToken = () => {

  }

  return {
    hashPassword,
    comparePasswords,
    generateToken,
    decodeToken,
  };
};
